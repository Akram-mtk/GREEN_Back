generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Users {
  id           String         @id @db.Uuid @default(uuid())
  first_name   String
  last_name    String
  email        String         @unique @db.VarChar(255)
  phone        String?        @db.VarChar(20)
  password     String         @db.VarChar(255)
  is_active    Boolean        @default(true)
  created_at   DateTime       @default(now()) @db.Timestamptz(0)
  EntryLog     EntryLog[]
  Rfid_cards   Rfid_cards[]
  Subscription Subscription[]
  Ticket       Ticket[]
}

model Area {
  id                      String                    @id @db.Uuid @default(uuid())
  name                    String
  EventCapacityAllocation EventCapacityAllocation[]
  Subscription            Subscription[]
  Ticket                  Ticket[]
}

model EntryLog {
  id       String   @id @db.Uuid @default(uuid())
  user_id  String   @db.Uuid
  event_id String   @db.Uuid
  date     DateTime
  Event    Event    @relation(fields: [event_id], references: [id])
  Users    Users    @relation(fields: [user_id], references: [id])

  @@unique([user_id, event_id])
}

model Event {
  id                      String                    @id @db.Uuid @default(uuid())
  name                    String
  any_gate_entry          Boolean
  open_at                 DateTime
  close_at                DateTime
  published               Boolean                   @default(false)
  EntryLog                EntryLog[]
  EventCapacityAllocation EventCapacityAllocation[]
  Ticket                  Ticket[]
}

model EventCapacityAllocation {
  id              String   @id @db.Uuid @default(uuid())
  event_id        String   @db.Uuid
  area_id         String   @db.Uuid
  available_seats Int
  Area            Area     @relation(fields: [area_id], references: [id])
  Event           Event    @relation(fields: [event_id], references: [id])
  Ticket          Ticket[]

  @@unique([event_id, area_id])
}

model Rfid_cards {
  id       String     @id @db.Uuid @default(uuid())
  owner_id String?    @db.Uuid
  card_uid String     @db.VarChar(64)
  status   CardStatus @default(issued)
  Users    Users?     @relation(fields: [owner_id], references: [id])
}

model Subscription {
  id       String   @id @db.Uuid @default(uuid())
  from     DateTime @db.Date
  to       DateTime @db.Date
  owner_id String   @db.Uuid
  area_id  String   @db.Uuid
  Area     Area     @relation(fields: [area_id], references: [id])
  Users    Users    @relation(fields: [owner_id], references: [id])
}

model Ticket {
  id                      String                  @id @db.Uuid @default(uuid())
  allocation_id           String                  @db.Uuid
  user_id                 String                  @db.Uuid
  confirmed               Boolean                 @default(false)
  used                    Boolean                 @default(false)
  areaId                  String?                 @db.Uuid
  event_id                String                  @db.Uuid
  EventCapacityAllocation EventCapacityAllocation @relation(fields: [allocation_id], references: [id])
  Area                    Area?                   @relation(fields: [areaId], references: [id])
  Event                   Event                   @relation(fields: [event_id], references: [id])
  Users                   Users                   @relation(fields: [user_id], references: [id])

  @@unique([event_id, user_id])
}

enum CardStatus {
  issued
  active
  blocked
}
